steps:
  # Install
- name: 'gcr.io/cloud-builders/npm'
  args: ['install']

  # Build
- name: 'gcr.io/cloud-builders/npm'
  args: ['run', 'build']

  #Deploy
- name: 'gcloud'
  args:
  - functions
  - deploy
  - mail
  - --runtime=nodejs14
  - --source=.
  - --trigger-http